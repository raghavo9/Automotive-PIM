{% extends 'base.html.twig' %}


{% block title %}New Product{% endblock %}


       
   

{% block body %}
{% if is_granted('ROLE_MANAGER') %}
    <h1>Create new Product</h1>

    {{ include('product/_form.html.twig') }}

    <a href="{{ path('product_index') }}">back to list</a>

    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}
    <h1>Create new Product</h1>

    {{ include('product/_form.html.twig') }}

    <a href="{{ path('product_index') }}">back to list</a>

    {% endif %}

    {% if is_granted('ROLE_USER') %}
     <button style="display:block; float:left;" onclick="window.location.href='http://localhost:8000/homepage';">Back to Homepage </button>
    <img  src="/uploads/images/not_found_error.jpeg" style="margin-left:20%; margin-top:15%">
    

    {% endif %}
{% endblock %}


 